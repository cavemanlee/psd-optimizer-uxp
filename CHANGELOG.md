# Changelog

## 1.8.3

- 修复 UXP 在说明页使用强制断词时裁切每行末尾中文字符的问题。
- 说明段落取消隐藏溢出，并预留右侧字形绘制空间，确保最小面板宽度下中英文混排完整显示。
- 增加回归检查，禁止恢复会触发行尾裁切的旧换行属性组合。

## 1.8.2

- 修复设置/说明页底部中英文说明文本在最小面板宽度下越过右侧边界并被裁切的问题。
- 将说明页和说明段落显式限制在可用内容宽度内，并允许纵向 Flex 子项正确收缩。
- 增加中文、英文、文件名及 Photoshop 名称混排的可靠换行规则。
- 增加说明文本宽度和换行规则的自动化回归检查，防止后续 UI 调整再次引入裁切。

## 1.8.1

- 将面板首选与最小宽度统一为 320px，并采用与同产品体系一致的背景、边框、强调色和状态色。
- 顶部改为 44px 标准标题栏，统一标题、左右边距、设置入口及图标点击区域。
- 将三个一级优化功能改为严格等宽卡片，统一 58px 高度、34px 图标与 8px 列间距。
- 设置/说明页关闭按钮替换为标准 `close-line` 图标，移除旧自绘交叉线的比例与渲染问题。
- 增加离线图标母版、确定性渲染步骤及面板尺寸、配色、图标和等宽布局回归检查。
- 构建前自动清理 `dist/` 中旧版本安装包和校验文件，避免本地制品持续累积。

## 1.8.0

- 将说明页版本号改为可点击的手动检查更新入口，并补充悬停、焦点和禁用状态。
- 新增独立的小型更新对话框，支持最新版、新版本、本地版本更高及分类错误提示。
- 通过 GitHub 公开 Release API 检查稳定版本，并使用固定 GitHub 下载页面，不自动下载或安装文件。
- 增加单请求锁、8 秒超时、取消、过期结果保护和 60 秒会话缓存。
- 更新检查不发送凭据；关闭说明页、隐藏面板或销毁插件时终止请求。
- 增加键盘焦点循环、Esc 关闭、双语稳定渲染及网络、外链权限声明。
- `_fix` 副本不再无条件启用最大兼容性；仅在 Photoshop 偏好设为“始终”时写入兼容合成图，“询问”和“从不”均不会被插件强制开启。
- 优化后体积增加时显示原因提示，区分 PSD 重写、兼容预览和最小 XMP 容器带来的正常变化。
- 优化后体积减小时，在原有前后体积、减少量和百分比之后明确显示“文件体积已减小”。

## 1.7.6

- 补齐面板 `create`、`show`、`hide`、`destroy` 生命周期，并增加 Photoshop `uxpcommand` 显示状态兜底。
- 防止快速重复点击并发启动多个优化任务。
- 用户取消 Photoshop 模态操作时立即停止并回滚，不再继续保存。
- 将图层遍历改为线性复杂度，降低大型 PSD 的卡顿和临时内存占用。
- 根据 Photoshop 版本启用 `executeAsModal.timeOut`，兼容 Photoshop 25.0–25.9。

## 1.7.5

- 将 Photoshop 折叠栏入口更新为扫把与三颗星芒图标。
- 提供深色、浅色主题以及 23×23、46×46 图标资源。
- 保留 Windows UXP 注册路径修复。
- 保留每次优化结果独立刷新、文件体积变化和安全保存机制。

## 1.7.4

- 修正“覆盖当前文档”的对号图标。
- 清理结果改为每次操作直接刷新，不跨操作累加。
- 调整说明页底部间距与说明入口图标。
- 增加 Windows 和 macOS 独立安装器。
